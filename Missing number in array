class Solution {
    int MissingNumber(int array[], int n) {
        // Your Code Here
    n = array.length + 1;
    int sum = (n * (n + 1)) / 2;
    int arrSum = 0;
    for (int i = 0; i < array.length; i++) {
        arrSum += array[i];
    }
    return sum - arrSum;
        
    }
}
